{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --host 0.0.0.0 --port 8080",
    "start": "uv run uvicorn main:app --host 0.0.0.0 --port 8080",
    "lint": "uv run ruff check . && uv run ruff format --check .",
    "lint:fix": "uv run ruff check --fix . && uv run ruff format .",
    "test": "uv run pytest",
    "build": "echo 'Python project: no build step'"
  }
}
